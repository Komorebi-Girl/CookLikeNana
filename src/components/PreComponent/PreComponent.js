import React, { Component } from "react";


const PreComponent = props => (
  <div>
          <div style={{height: '90px', padding: '10px', position: 'relative'}}>
          </div>
      </div>
);

export default PreComponent;
